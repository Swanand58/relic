# Knowledge Index

This directory contains AI-generated knowledge graphs for each subproject.
Each subdirectory corresponds to a subproject defined in `relic.yaml`.

## Subprojects

| Name | Graph | Description |
|------|-------|-------------|
| _(add entries here as you run `relic --refresh`)_ | | |

## Usage

Load a subproject's graph into your clipboard:
```
relic payments
```

Load multiple at once:
```
relic payments api
```

Regenerate a graph after code changes:
```
relic --refresh payments
```
